package com.game.drunner.utils;

import mygame.gameframework.utils.Graphics;

public interface Drawables {

	/**
	 * Interface: is passed a Graphic object so that content can be drawn within the class 
	 * implementing class.
	 * @param g = Graphics
	 */
	public void draw(Graphics g);
}
